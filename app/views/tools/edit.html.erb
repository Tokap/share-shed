<main>
  <section>
  </section>

  <article>
    <div class='new-tool white-div'>
      <h1>Edit tool</h1>
      <% if @tool.errors.any? %>
        <% @tool.errors.full_messages.each do |error| %>
          <ul>
            <li class='error-msg'><%= error %></li>
          </ul>
        <% end %>
      <% end  %>
      <%= render 'form' %>
    </div>
  </article>

  <aside>
  </aside>
</main>
