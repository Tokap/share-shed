<h2>Select a <%= @abstract_tool.name %> to view its details</h2>

<ul>
  <% @tools.each do |tool| %>
    <article>
      <% if tool.available %>
        <li><%= link_to "$#{tool.base_price} - #{GoogleDirections.new(current_user.address, tool.owner.address).drive_time_in_minutes}" , tool_path(tool) %> </li>
      <% end %>
    </article>
  <% end %>
</ul>

