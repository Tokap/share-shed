<hgroup class="dashboard">

</hgroup>

<main class="dashboard">

  <section>
    <ul class="tab">
      <li><a href="#" class="toolshed tabLinks active">Your Toolshed</a></li>
      <li><a href="#" class="rentals tabLinks">Open Rentals</a></li>
      <li><a href="#" class="feedback tabLinks">Feedback</a></li>
      <li><a href="#" class="history tabLinks">Rental History</a></li>
    </ul>

    <div id="toolshed" class="tabContent">
      <div class='flex-container'>
        <% @owned_tools.each do |tool| %>
          <a href="/tools/<%= tool.id %>">
          <% if tool.available %>
            <div class='tool-card'>
              <% if tool.image.file? %>
                <img class='tool-pic' src="<%= tool.image.url %>">
              <% else %>
                <img class='tool-pic' src="/assets/shovel.png">
              <% end %>
              <h4><%= tool.abstract_tool.name %></h4>
            </div>
          <% else %>
            <div class='tool-card unavailable'>
              <% if tool.image.file? %>
                <img class='tool-pic unavailable' src="<%= tool.image.url %>">
              <% else %>
                <img class='tool-pic unavailable' src="/assets/shovel.png">
              <% end %>
              <h4 class="unavailable"><%= tool.abstract_tool.name %></h4>
            </div>
          <% end %>
          </a>
        <% end %>
      </div>
    </div>

    <div id="rentals" class="tabContent">
      <p>Here will be a list of the rentals you have, sectioned into two categories: as an owner and as a renter</p>
    </div>

    <div id="feedback" class="tabContent">
      <p>Here will be feedback that has been submitted about you, and feedback that you have submitted about other people</p>
    </div>

    <div id="history" class="tabContent">
      <p>Here will be a list of all your closed rental agreements</p>
    </div>
  </section>





















  <aside>
     <h3>You have a rental request from:</h3>
     <% if @owned_rentals.any? %>
      <ul>
      <% @owned_rentals.each do |o_rental|  %>
        <% unless o_rental.status == "draft" %><li><%= link_to "#{o_rental.renter.username} -- #{o_rental.status}", rental_path(o_rental) %></li><% end %>
      <% end %>
    </ul>
    <% end %>
    <h3>You have open rental requests with:</h3>
    <% if @rented_rentals.any? %>
    <ul>
      <% @rented_rentals.each do |r_rental|  %>
        <li><%= link_to "#{r_rental.owner.username} -- #{r_rental.status}", rental_path(r_rental) %></li>
      <% end %>
    </ul>
    <% end %>
  </aside>

</main>
